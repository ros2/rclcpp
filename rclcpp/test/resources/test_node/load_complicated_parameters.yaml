/**:
  ros__parameters:
    bar: 5
    foo: 3.5
    a_value: "first"

/*:
  load_node:
    ros__parameters:
      bar_foo: "ok"
      a_value: "second"

namespace:
  load_node:
    ros__parameters:
      foo_bar: true
      a_value: "third"

bar:
  ros__parameters:
    fatal: 10

/**/namespace/*:
  ros__parameters:
    a_value: "last_one_win"
